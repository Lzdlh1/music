# Music Frontend

Dev:
1. npm install
2. npm run dev (served on http://localhost:12233)

Notes:
- China Mobile 云盘等的 Cookie 请在 Settings 页面填写，保存在 localStorage，仅在创建任务时随请求发送给后端。
- The frontend expects the backend API at http://localhost:12233/api by default; override with VITE_API_BASE.
